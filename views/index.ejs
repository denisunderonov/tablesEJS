<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tables Example</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th,
    td {
      border: 1px solid black;
      padding: 8px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
    tr:nth-child(even) {
      background-color: #f2f2f2;
    }
  </style>
  <body>
    <h1>Андронов Денис 231-322</h1>

    <h2>Таблица "Книги"</h2>
    <a href="/addBook"><button>Add New Book</button></a>
    <table>
      <thead>
        <tr>
          <th>ISBN</th>
          <th>Название</th>
          <th>Автор</th>
          <th>Цена</th>
          <th>Год издания</th>
        </tr>
      </thead>
      <tbody>
        <% books.forEach(book => { %>
        <tr>
          <td><%= book.isbn %></td>
          <td><%= book.Название %></td>
          <td><%= book.Автор %></td>
          <td><%= book.Цена_книги %></td>
          <td><%= book.Год_издания %></td>
          <td>
            <a href="/updateBook/<%= book.isbn %>"
              ><button>Update Book</button></a
            >
          </td>
          <td>
            <form action="/deleteBook/<%= book.isbn %>" method="POST">
              <input type="hidden" name="_method" value="DELETE" />
              <button type="submit">Delete Book</button>
            </form>
          </td>
        </tr>
        <% }); %>
      </tbody>
    </table>

    <h2>Таблица "Выдачи"</h2>
    <a href="/addIssue"><button>Add New Issue</button></a>
    <table>
      <thead>
        <tr>
          <th>ID Выдачи</th>
          <th>ID Читателя</th>
          <th>ISBN</th>
          <th>ID Библиотекаря</th>
          <th>Дата Выдачи</th>
          <th>Дата Возврата</th>
        </tr>
      </thead>
      <tbody>
        <% issues.forEach(issue => { %>
        <tr>
          <td><%= issue.id_выдачи %></td>
          <td><%= issue.id_читателя %></td>
          <td><%= issue.isbn %></td>
          <td><%= issue.id_библиотекаря %></td>
          <td><%= issue.Дата_выдачи %></td>
          <td><%= issue.Дата_возврата %></td>
          <td>
            <a href="/updateIssue/<%= issue.isbn %>"
              ><button>Update Issue</button></a
            >
          </td>
          <td>
            <form action="/deleteIssue/<%= issue.isbn %>" method="POST">
              <input type="hidden" name="_method" value="DELETE" />
              <button type="submit">Delete Issue</button>
            </form>
          </td>
        </tr>
        <% }); %>
      </tbody>
    </table>
  </body>
</html>
